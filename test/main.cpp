#define CATCH_CONFIG_MAIN
#include <catch2/catch.hpp>

// All tests are included in separate files in this directory. This allows
// the Catch framework to be compiled separately, to avoid having to recompile
// it when making changes to tests.
